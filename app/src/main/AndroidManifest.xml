<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myschoolapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopUpSubject">

        </activity>
        <activity android:name=".PopUpTeacher" >



        </activity>
        <activity android:name=".PopUpStudent" >

        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Subjects" />
        <activity android:name=".Teachers" />
        <activity android:name=".LogInSingUp">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Student" />
    </application>

</manifest>